Improved: the per-supply stock breakdown on a period card is now a tap-to-view popup (an "i" icon next to Stock at start/end) instead of an always-shown list, keeping the card compact when a drug has multiple supplies.
