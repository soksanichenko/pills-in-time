Improved: hourly session reminders (e.g. eye drops) now post as regular dismissible notifications instead of one that stays pinned all day.
New: session reminders (day-start prompt, hourly, and count-per-day) each have their own notification channel, so you can mute them independently.
Improved: switch a period's time between fixed and session-based dosing in place, and pick its repeat cycle from a dropdown.
